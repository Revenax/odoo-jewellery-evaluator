# Local deploy (./scripts/deploy-local.sh or make deploy-local).
# Copy to .env and set values. EC2_SSH_KEY = full private key (multi-line PEM or base64).
EC2_HOST=your-ec2-hostname-or-ip
EC2_USER=ubuntu
EC2_SSH_KEY=-----BEGIN OPENSSH PRIVATE KEY-----
...paste key lines or single-line base64...
-----END OPENSSH PRIVATE KEY-----
# Repo path on the server (clone/pull and run upgrade here)
EC2_GIT_REPO_PATH=/opt/odoo/custom-addons/jewellery_evaluator

# Revenax Pulse — notifications. Ask Revenax for these.
REVENAX_PULSE_SERVICE_NAME=
REVENAX_PULSE_API_KEY=
